# 이 파일을 복사해서 같은 폴더에 secrets.toml 이라는 이름으로 저장한 뒤,
# 아래 값을 본인의 실제 OpenAI API 키로 바꿔주세요.
#
# secrets.toml은 .gitignore에 포함되어 있어서 GitHub에는 올라가지 않아요.
# (실제 키가 들어있는 파일이 저장소에 올라가지 않도록 항상 주의하세요!)

OPENAI_API_KEY = "sk-여기에-실제-키를-입력하세요"
